<h3><%= t '.welcome' %></h3>
<p>
    <%= t '.intro' %> 
</p>
<p>
    <%= t '.call_to_action_html', :signup_link => link_to(t('.create_account'), new_user_registration_path) %>
</p>

<h3><%= GeoGraph.model_name.human :count => 2 %></h3>
<table>
    <thead>
        <tr>
            <th><%= GeoGraph.human_attribute_name(:name) %></th>
            <th><%= GeoGraph.human_attribute_name(:updated) %></th>
            <% if user_signed_in? %>
                <th></th>
            <% end %>
        </tr>
    </thead>
    <tbody>
        <%= render :partial => 'shared/line_items/geo_graph', :collection => @geographs %>
    </tbody>
</table>
<% if user_signed_in? %>
    <%= link_to "Create a new geograph", new_geo_graph_path %>
<% end %>
